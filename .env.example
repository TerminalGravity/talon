# Talon Environment Variables

# Required: OpenClaw Gateway URL
GATEWAY_URL=https://your-gateway.example.com:5050

# Required: Gateway authentication token
GATEWAY_TOKEN=your-gateway-token

# Required for semantic search/embeddings
OPENAI_API_KEY=sk-your-openai-key

# Optional: LanceDB path (defaults to .lancedb in project root)
LANCEDB_PATH=./.lancedb
